// const verifyAdmin = (req,res) => {
//     if(req.session.user.title === 'admin' || req.session.user.title === 'manager') return res.status(200).send(req.session.user.title)
//     return 'unauthorized_user'
// }

// const verifyManager = (req, res) => {
// }

// module.exports = {
//     verifyAdmin,
//     verifyManager
// }